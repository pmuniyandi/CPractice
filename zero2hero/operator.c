// Single line comments, important logic 
#include<stdio.h>

/*
this program written for operator related logic to be cover
Date: <>
Author: <>
version 1

vers

assignment, =
arithematic  +, -, * / %
logic && || !
relational <, > <= >= == !=
bit &, | >> <<
conditional  ? :  a>b?a:b
auto incr/de ++, --, => per and post
a = a + 10 => a += 10
 % 

 expersion =>  a + b => + (a, b)\
 // auto
 // manual -> upwards, downward

*/

/*

int main() {
    float number1 = 3.2;
    float number2 = 10;

    float res = number1 + number2;

    printf("%.2f, %.3f, %d\n", res , number1 / number2, (int)res);
    printf("%d, %d\n", number1 > number2, number1 < number2);

    printf("%d\n", (number1 > number2 && number1 < number2));
    printf("%d\n", (number1 > number2 || number1 < number2));
    printf("%d\n", !(number1 > number2 || number1 < number2));

    printf("between %f and %f, is greater %f\n",number1, number2, (number1 > number2?number1:number2));

}
*/

int main() {
    int a = 10;
    //(a & 2) && printf("%d is Even\n", a);
    //(a % 2 != 0) && printf("%d is Odd\n", a);
}



